function solve(firtstNumber, secondNumber, thirdNumber) {
    'use strict';

    const smaallestNumber = Math.min(firtstNumber, secondNumber, thirdNumber)
    console.log(smaallestNumber)
}
solve(2,5,3);